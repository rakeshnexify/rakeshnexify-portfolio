{
  "name": "rakeshnexify-portfolio",
  "version": "1.0.0",
  "description": "Production-ready MERN portfolio and content management system for RakeshNexify.",
  "main": "server/src/server.js",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently --kill-others-on-fail \"npm run client\" \"npm run server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "setup:dependencies": "npm install --prefix client && npm install --prefix server",
   "check": "npm run build && node --check server/src/config/environment.js && node --check server/src/config/cors.js && node --check server/src/config/helmet.js && node --check server/src/app.js && node --check server/src/server.js",
    "test": "npm run check"
  },
  "keywords": [
    "mern",
    "portfolio",
    "react",
    "express",
    "mongodb",
    "wordpress",
    "cms"
  ],
  "author": "Rakesh Pandit",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  },
  "private": true
}
